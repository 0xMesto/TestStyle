<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>CardCraft - Enhanced Accessible Card Component</title>
  <%- include('partials/head') %>
</head>
<body class="bg-gradient-to-br from-indigo-50 via-purple-50 to-pink-50 flex flex-col min-h-screen font-sans text-gray-800 dark:text-gray-200 dark:bg-gray-900">
  
  <%- include('partials/header') %>
  <%- include('sections/hero') %>
  <main class="flex-grow flex items-center justify-center p-4">
    <div x-data="{ expanded: false, liked: false, bookmarked: false, rating: 0, showTooltip: false }" 
         class="max-w-md w-full rounded-3xl overflow-hidden card-shadow bg-white transition-all duration-300 ease-in-out dark:bg-gray-800 dark:text-white"
         :class="{ 'sm:hover:scale-105': !expanded, 'sm:animate-[float_6s_ease-in-out_infinite]': !expanded }"
         @keydown.escape="expanded = false"
         role="article"
         aria-labelledby="card-title">
        <div class="relative overflow-hidden group">
            <img class="w-full h-64 sm:h-72 object-cover transition-transform duration-500 ease-in-out group-hover:scale-105" src="https://source.unsplash.com/random/800x600" alt="Decorative card image showing a random scene" loading="lazy">
            <div class="absolute inset-0 bg-gradient-to-t from-black/70 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-end justify-start p-6">
                <p class="text-white text-xl font-semibold transform translate-y-4 group-hover:translate-y-0 transition-transform duration-300">Discover the Magic</p>
            </div>
            <div class="absolute top-4 right-4 bg-gradient-to-r from-yellow-400 to-orange-500 text-white px-3 py-1 rounded-full text-xs font-bold shadow-lg transform rotate-2 group-hover:rotate-0 transition-transform duration-300 animate-[pulse_2s_ease-in-out_infinite]" aria-hidden="true">New</div>
        </div>
        <div class="px-6 py-4">
            <h2 id="card-title" class="font-bold text-2xl sm:text-3xl mb-3 text-gray-800 group-hover:text-indigo-600 transition-colors duration-300 dark:text-white dark:group-hover:text-indigo-400">Enhanced Accessible Card Design</h2>
            <p class="text-gray-600 text-sm sm:text-base leading-relaxed dark:text-gray-300">
                This card showcases advanced Tailwind CSS techniques, improved accessibility, and sophisticated user interactions. It demonstrates fluid responsiveness, captivating animations, and dynamic content management for an enhanced and inclusive user experience.
            </p>
        </div>
        <div class="px-6 pt-4 pb-6 flex flex-wrap justify-between items-center gap-4">
            <button @click="expanded = !expanded" 
                    class="bg-gradient-to-r from-indigo-500 to-purple-600 hover:from-indigo-600 hover:to-purple-700 text-white font-bold py-2 px-6 rounded-full transition duration-300 ease-in-out transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-indigo-400 focus:ring-opacity-50 shadow-md focus-visible micro-bounce" 
                    :aria-expanded="expanded" 
                    aria-controls="additional-info">
                <span x-text="expanded ? 'Hide Details' : 'Show Details'"></span>
            </button>
            <div class="flex flex-wrap gap-2" aria-label="Tags">
                <span class="inline-block bg-indigo-100 text-indigo-800 rounded-full px-3 py-1 text-xs sm:text-sm font-semibold dark:bg-indigo-800 dark:text-indigo-100 transition-transform duration-300 hover:scale-110 micro-rotate">#design</span>
                <span class="inline-block bg-purple-100 text-purple-800 rounded-full px-3 py-1 text-xs sm:text-sm font-semibold dark:bg-purple-800 dark:text-purple-100 transition-transform duration-300 hover:scale-110 micro-rotate">#ui</span>
                <span class="inline-block bg-pink-100 text-pink-800 rounded-full px-3 py-1 text-xs sm:text-sm font-semibold dark:bg-pink-800 dark:text-pink-100 transition-transform duration-300 hover:scale-110 micro-rotate">#ux</span>
            </div>
        </div>
        <div x-show="expanded" 
             x-transition:enter="transition ease-out duration-300" 
             x-transition:enter-start="opacity-0 transform scale-95" 
             x-transition:enter-end="opacity-100 transform scale-100" 
             x-transition:leave="transition ease-in duration-200" 
             x-transition:leave-start="opacity-100 transform scale-100" 
             x-transition:leave-end="opacity-0 transform scale-95" 
             class="px-6 py-4 bg-gray-50 dark:bg-gray-700" 
             id="additional-info">
            <h3 class="font-semibold text-lg sm:text-xl mb-3 text-gray-700 dark:text-gray-200">Additional Information</h3>
            <ul class="list-disc list-inside text-gray-600 space-y-2 dark:text-gray-300 text-sm sm:text-base">
                <li>Fluid responsive design for all devices</li>
                <li>Enhanced accessibility with ARIA attributes and keyboard navigation</li>
                <li>Captivating animations and smooth transitions</li>
                <li>Dynamic content toggling with Alpine.js</li>
                <li>Refined visual hierarchy and color scheme</li>
                <li>Dark mode support for improved user experience</li>
                <li>Advanced hover effects and micro-interactions</li>
            </ul>
        </div>
        <div class="px-6 py-4 bg-gray-50 flex flex-wrap justify-between items-center gap-4 dark:bg-gray-700">
            <div class="flex items-center space-x-3">
                <img class="w-10 h-10 sm:w-12 sm:h-12 rounded-full object-cover ring-2 ring-indigo-300 dark:ring-indigo-600 transition-transform duration-300 hover:scale-110" src="https://source.unsplash.com/random/100x100" alt="Author avatar" loading="lazy">
                <div>
                    <span class="text-sm text-gray-600 font-medium dark:text-gray-300">Jane Smith</span>
                    <p class="text-xs text-gray-500 dark:text-gray-400">UI/UX Designer</p>
                </div>
            </div>
            <div class="flex items-center space-x-4">
                <button @click="liked = !liked" 
                        class="text-gray-500 hover:text-red-500 transition-colors duration-300 focus:outline-none focus:ring-2 focus:ring-red-400 rounded-full p-2 focus-visible micro-bounce" 
                        :class="{ 'text-red-500': liked }" 
                        :aria-label="liked ? 'Unlike' : 'Like'"
                        :aria-pressed="liked">
                    <i class="fas" :class="liked ? 'fa-heart' : 'fa-heart'"></i>
                    <span class="sr-only" x-text="liked ? 'Unlike' : 'Like'"></span>
                </button>
                <button @click="bookmarked = !bookmarked" 
                        class="text-gray-500 hover:text-indigo-500 transition-colors duration-300 focus:outline-none focus:ring-2 focus:ring-indigo-400 rounded-full p-2 focus-visible micro-bounce" 
                        :class="{ 'text-indigo-500': bookmarked }" 
                        :aria-label="bookmarked ? 'Remove bookmark' : 'Bookmark'"
                        :aria-pressed="bookmarked">
                    <i class="fas" :class="bookmarked ? 'fa-bookmark' : 'fa-bookmark'"></i>
                    <span class="sr-only" x-text="bookmarked ? 'Remove bookmark' : 'Bookmark'"></span>
                </button>
                <button @mouseenter="showTooltip = true" 
                        @mouseleave="showTooltip = false" 
                        @focus="showTooltip = true"
                        @blur="showTooltip = false"
                        class="text-gray-500 hover:text-green-500 transition-colors duration-300 focus:outline-none focus:ring-2 focus:ring-green-400 rounded-full p-2 relative focus-visible micro-bounce"
                        aria-label="Share">
                    <i class="fas fa-share-alt"></i>
                    <span class="sr-only">Share</span>
                    <div x-show="showTooltip" 
                         x-transition:enter="transition ease-out duration-200" 
                         x-transition:enter-start="opacity-0 transform scale-95" 
                         x-transition:enter-end="opacity-100 transform scale-100" 
                         x-transition:leave="transition ease-in duration-150" 
                         x-transition:leave-start="opacity-100 transform scale-100" 
                         x-transition:leave-end="opacity-0 transform scale-95" 
                         class="absolute bottom-full left-1/2 transform -translate-x-1/2 mb-2 px-3 py-1 bg-gray-800 text-white text-xs rounded-lg shadow-lg"
                         role="tooltip">
                        Share this card
                    </div>
                </button>
            </div>
        </div>
        <div class="px-6 py-3 bg-gray-100 text-xs sm:text-sm text-gray-500 flex flex-wrap justify-between items-center gap-4 dark:bg-gray-600 dark:text-gray-300">
            <span>Last updated: 1 day ago</span>
            <div class="flex items-center space-x-2">
                <i class="fas fa-eye text-indigo-500" aria-hidden="true"></i>
                <span>2.5k views</span>
            </div>
        </div>
        <div class="px-6 py-4 bg-gray-50 dark:bg-gray-700">
            <h4 id="rating-label" class="font-semibold text-base sm:text-lg mb-3 text-gray-700 dark:text-gray-200">Rate this card</h4>
            <div class="flex items-center space-x-1" role="radiogroup" aria-labelledby="rating-label">
                <template x-for="star in 5">
                    <button @click="rating = star" 
                            class="text-2xl sm:text-3xl focus:outline-none focus:ring-2 focus:ring-yellow-400 rounded-full p-1 transition-transform duration-200 hover:scale-110 focus-visible micro-bounce" 
                            :class="star <= rating ? 'text-yellow-400' : 'text-gray-300'"
                            :aria-label="`${star} star${star !== 1 ? 's' : ''}`"
                            role="radio"
                            :aria-checked="rating === star">
                        ★
                        <span class="sr-only" x-text="`${star} star${star !== 1 ? 's' : ''}`"></span>
                    </button>
                </template>
            </div>
        </div>
    </div>
  </main>
  <%- include('partials/footer') %>
  <script>
    // Enhance keyboard navigation
    document.addEventListener('keydown', function(e) {
        if (e.key === 'Tab') {
            document.body.classList.add('user-is-tabbing');
        }
    });
    document.addEventListener('mousedown', function() {
        document.body.classList.remove('user-is-tabbing');
    });
  </script>
</body>
</html>
